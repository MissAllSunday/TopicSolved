<?xml version="1.0"?>
<!DOCTYPE modification SYSTEM "http://www.simplemachines.org/xml/modification">
<modification xmlns="http://www.simplemachines.org/xml/modification" xmlns:smf="http://www.simplemachines.org/">
	<id>Suki:TopicSolved</id>
	<version>1.2</version>
	<file name="$sourcedir/Admin.php">

		<!-- There is no hook for this on 2.0... -->
		<operation>
			<search position="replace"><![CDATA[$sub_action =]]></search>
			<add><![CDATA[call_integration_hook('integrate_manage_logs', array($log_functions));

	$sub_action =]]></add>
		</operation>
	</file>

	<file name="$sourcedir/MessageIndex.php">
		<operation>
			<search position="replace"><![CDATA[t.id_topic, t.num_replies, t.locked, t.num_views, t.is_sticky, t.id_poll, t.id_previous_board,]]></search>
			<add><![CDATA[t.id_topic, t.num_replies, t.locked, t.num_views, t.is_sticky, t.id_poll, t.id_previous_board, is_solved,]]></add>
		</operation>
		<operation>
			<search position="replace"><![CDATA['link' => '<a href="' . $scripturl . '?topic=' . $row['id_topic'] . '.0">' . $row['first_subject'] . '</a>']]></search>
			<add><![CDATA['link' => '<a href="' . $scripturl . '?topic=' . $row['id_topic'] . '.0">' . $row['first_subject'] . '</a>',
					'is_solved' => $row['is_solved'],]]></add>
		</operation>
	</file>
